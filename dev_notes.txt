minio_creds
{
    "MINIO_ROOT_USER": "admin",
    "MINIO_ROOT_PASSWORD": "admin123",
    "MINIO_ENDPOINT": "minio:9000",
    "MINIO_KMS_SECRET_KEY": "my-minio-key:YWRtaW4xMjNhZG1pbjEyM2FkbWluMTIzYWRtaW4xMjM=",
    "MINIO_BUCKET_RAW": "raw",
    "MINIO_BUCKET_STAGING": "staging",
    "MINIO_BUCKET_CURATED": "curated"
}

postgresql_creds
{
    "POSTGRES_USER": "postgres",
    "POSTGRES_PASSWORD": "admin123",
    "POSTGRES_DB": "postgresdb",
    "POSTGRES_PORT": "5433"
}

airflowdb_creds
{
    "AIRFLOW_DB_USER": "airflow",
    "AIRFLOW_DB_PASS": "airflow",
    "AIRFLOW_DB_NAME": "airflow",
    "AIRFLOW_DB_PORT": "5434"
}

python main_etl_runner.py --processes all --data_path data/weather_data_sample.json

python render_scripts.py --config etl_config.yaml --mode etl
python render_scripts.py --config process_config.yaml --mode process

docker-compose up --build

docker-compose down

tHEKbaz9xeQmur8P

D:
cd workspace\airflow